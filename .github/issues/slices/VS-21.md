---
title: "VS-21: Scientific Mode Calculator"
labels: ["slice:progressive", "priority:medium", "type:feature", "phase:v3.0"]
assignees: []
---

## Overview

**Slice Type:** Progressive Enhancement
**Priority:** Medium
**Effort:** 16 hours
**Risk:** Medium
**Dependencies:** VS-15 (expression display)

Add scientific calculator mode with trigonometric, logarithmic, and exponential functions.

## User Story

```
As a student or professional
I want scientific calculator functions
So that I can perform advanced mathematical operations
```

## Acceptance Criteria

- [ ] Toggle between standard and scientific mode
- [ ] Sin, Cos, Tan functions
- [ ] Log, Ln functions
- [ ] Exponential (e^x) functions
- [ ] Degree/Radian mode toggle
- [ ] Scientific notation display
- [ ] Additional buttons for scientific functions
- [ ] π and e constants available
- [ ] Power (x^y) function
- [ ] Factorial function

## Technical Requirements

**Scientific Functions:**

```javascript
const scientificOps = {
  sin: (x, angleMode) => {
    const rad = angleMode === "deg" ? (x * Math.PI) / 180 : x;
    return Math.sin(rad);
  },
  cos: (x, angleMode) => {
    const rad = angleMode === "deg" ? (x * Math.PI) / 180 : x;
    return Math.cos(rad);
  },
  tan: (x, angleMode) => {
    const rad = angleMode === "deg" ? (x * Math.PI) / 180 : x;
    return Math.tan(rad);
  },
  log: (x) => Math.log10(x),
  ln: (x) => Math.log(x),
  exp: (x) => Math.exp(x),
  power: (base, exponent) => Math.pow(base, exponent),
  factorial: (n) => {
    if (n < 0 || !Number.isInteger(n)) {
      return {
        error: true,
        message: "Factorial only for non-negative integers",
      };
    }
    let result = 1;
    for (let i = 2; i <= n; i++) result *= i;
    return result;
  },
};
```

**UI Layout:**
Extended button grid with scientific functions

**Mode State:**

- Standard mode / Scientific mode toggle
- Degree / Radian mode toggle

## Implementation Checklist

- [ ] Implement trigonometric functions
- [ ] Implement logarithmic functions
- [ ] Implement exponential functions
- [ ] Implement power function
- [ ] Implement factorial function
- [ ] Add π and e constants
- [ ] Create mode toggle UI
- [ ] Extend button layout for scientific mode
- [ ] Implement degree/radian conversion
- [ ] Add scientific notation display
- [ ] Write unit tests for all functions
- [ ] Test angle mode conversions
- [ ] Test edge cases
- [ ] Update expression display for scientific ops
